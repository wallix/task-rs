version: '3'

vars:
  CACHE_DIR:
    sh: echo "$CACHE_DIR"

tasks:
  build:
    sources:
      - input.txt
    generates:
      - output.txt
    cache:
      enabled: false
      url: 'file://{{.CACHE_DIR}}/build-{{.CHECKSUM}}.zip'
    cmds:
      - cp input.txt output.txt
