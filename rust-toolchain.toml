# Pin the toolchain so every developer and CI build with the same compiler.
# build.sh uses the container's native musl target. Cross targets are added by
# the CI jobs that need them.
[toolchain]
channel = "1.97.1"
components = ["clippy", "rustfmt"]
profile = "minimal"
